# PostgreSQL connection
DATABASE_URL=postgres://rodb:rodb@localhost:5432/rodb

# API server
PORT=3000
HOST=0.0.0.0

# Default locale for responses when ?locale= is not provided.
# Supported: pt-BR (Brasil), es (LATAM espanhol), en (fallback)
DEFAULT_LOCALE=pt-BR
